[package]
name = "luthien-terminal"
description = "Luthien plugin which applies colors to terminals."
version = "0.1.0"
authors = ["Max Niederman <max@maxniederman.com>"]
license = "MIT"
edition = "2018"

[dependencies]
luthien-plugin = { path = "../lib/rust" }
